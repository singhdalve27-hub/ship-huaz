<script setup>
import { computed } from 'vue';
import { Link } from '@inertiajs/vue3';

const props = defineProps({
    href: {
        type: String,
        required: true,
    },
    active: {
        type: Boolean,
    },
});

const classes = computed(() =>
    props.active
        ? 'block w-full ps-3 pe-4 py-2 border-l-4 border-orange-500 text-start text-base font-bold text-sky-900 bg-sky-50 focus:outline-none focus:text-sky-900 focus:bg-sky-100 focus:border-orange-600 transition duration-150 ease-in-out'
        : 'block w-full ps-3 pe-4 py-2 border-l-4 border-transparent text-start text-base font-medium text-slate-600 hover:text-sky-900 hover:bg-slate-50 hover:border-slate-300 focus:outline-none focus:text-sky-900 focus:bg-slate-50 focus:border-slate-300 transition duration-150 ease-in-out',
);
</script>

<template>
    <Link :href="href" :class="classes">
        <slot />
    </Link>
</template>